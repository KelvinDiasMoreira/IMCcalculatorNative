import {StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    containerResult: {
        justifyContent: 'center',
        marginTop: 45,
        width: '90%',
        alignItems: 'center',
    },
    textResult : {
        fontSize: 50,
        color: '#334139',
        fontWeight: '900',
    },
  });